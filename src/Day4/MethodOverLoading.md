## Method Overloading 
It is Also called - Compile-time polymorphism
During compile time, the compiler determines which method to call based on the number and type of arguments passed to the method.
In method overloading, you can have multiple methods with the same name but different parameter lists.
The compiler uses the number and type of arguments to determine which method to call at compile time.